# Example only — copy to keystore.properties and fill locally.
# NEVER commit the real keystore.properties file or the .jks/.keystore.

storeFile=D:/PetProject/secure/logicGames/logic-games-release.jks
storePassword=REPLACE_ME
keyAlias=logicgames
keyPassword=REPLACE_ME
